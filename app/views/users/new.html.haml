#main_without_left_menu.span-22.prefix-1.suffix-1.last
  %h1 Register
  - semantic_form_for @user, :url => account_path do |f|
    = f.error_messages
    = render :partial => "users/form", :object => f
    - f.buttons do
      = f.commit_button "Register"
